- name: nginx install and run
  hosts: web
  become: yes # takes sudo access
  tasks: 
  - name: install nginx
    ansible.builtin.package: # heterogenious module that can work for all distributions
      name: nginx
      state: present
      